# 上云推进续接提示词（2026-09-23·阶段4 进行中版）

> 用途：本文件是**当前唯一执行口径**，取代 `上云推进续接提示词-2026-09-22.md` 与
> `上云推进续接提示词-2026-09-22-晚.md`。重启/新会话后把 §A 整段贴给执行 agent 即可无缝续接。
> 主任务表：`docs/上云更新方案-2026-09-21.md` §3 + `docs/阶段4切换执行checklist.md`。

---

## §0 进度总表（2026-09-23 05:00 对齐，远程实测口径）

| 事项 | 状态 |
|---|---|
| 方案 §3 第 1-7 步 | ✅ 全部完成（T7 冒烟通过，三轮稳定性验收） |
| 容器化 | ✅ `dops-app:latest` 镜像已建；`dingtalk-gateway`（Stream+投递，host 网络，unless-stopped）与 `dops-scheduler`（Nacos cron 驱动）两容器在跑；venv gateway 已退役 |
| B1 regions 真值 | ✅ 闭环（6 区域 seed + Nacos 回读 OK；新报数机器人 Stream 4 项实测） |
| B2/B3/B4/B5 | ✅ B2 解除阻塞；B3 已入 CI/CD（分支保护已修，PR #1-#6 全绿合并）；B4 收口完成；B5 celia 可随时派 |
| 阶段4 第 0-2 步 | ✅ 预检过（robot_outbox 空、凭据齐）、第 1 步发布（B1 提前完成）、第 2 步双跑（杭榜逐字一致；未填口径=通讯录定调；sync 时滞已识别） |
| **阶段4 第 3 步（G3 真群对照）** | 🔶 **进行中**：gateway 在跑、绍兴群门禁验证过（hq 被拒✅）、admin grant 已自举（王城）；🔴 **生产投递卡 `Card.Instance.Write` 403**（新应用未开互动卡片权限） |
| 机器人切换 | 新报数机器人已进全部 7 群；绍兴群旧机器人已剔除、**绍兴已单轨**（新机器人唯一报数入口）；杭州群是最后一个迁移对象 |
| vanke 多板块报数 | ✅ 解析器上线（门店×{零售,团购}、部门门禁、模糊补全）；月目标已置 0（今年无考核）；实战写入验证未做 |
| 电商渠道日报 | ✅ 三段式上线（渠道快报 DB 复刻+人员榜+进度头，样张与 AI 表一致）；调度 `channel-daily-qudao` 8:30 已发布；ecom 人员 402 行在库 |
| 双跑 | 杭州榜单 ✅；未填名单口径已定；旧渠道播报/现网杭州 20:00 催办对照未做 |

**当前推进序列（按序）**：① 开 Card 权限 → 投递恢复 + vanke/杭州实战验证 → ② 双跑收尾 → ③ G4（2 个工作日观察后注释现网 cron）。

---

## §A 可复制执行提示词

> 复制以下整段给执行 agent。

````text
你是 cloud-ops-agent，续接 digital-ops 天翼云上云任务。阶段4 第 3 步（G3）进行中，
gateway/scheduler 容器已在 dops-app 常驻。从「第①步」接着做；标注【闸口】的等运维。

【先读（5 分钟建立上下文）】
1. docs/上云推进续接提示词-2026-09-23.md（本提示词出处，进度总表）
2. docs/阶段4切换执行checklist.md（当前在第 3 步，G1-G5 闸口）
3. docs/superpowers/plans/2026-09-22-cloud-cutover-log.md（T7+B1 全程执行记录）
4. docs/superpowers/specs/2026-09-22-de-ai-table-db-single-source.md（去表格路线：
   通讯录为准、P0-P3、§7 群公告）
5. docs/superpowers/specs/2026-09-23-ecom-daily-report-and-sku-dashboard.md（电商日报
   三段式与 SKU 仪表盘设计）

【环境事实（已验证，直接用）】
- 三台 ECS 免密 SSH（root）：dops-app=203.205.91.241（.0.3）、dops-ctrl=203.205.93.194（.0.4）、
  dops-ci=203.205.93.195（.0.5）；RDS 192.168.0.7:13049（root/yyhl-tset123）；
  Nacos 192.168.0.4:8848（仅内网）
- dops-app：/opt/dops/repo（代码，bind 挂载进容器）、/opt/dops/venv、/opt/dops/app.env（600）、
  /opt/dops/live/（600：source-credentials/source-manifest/gateway-credentials/
  regions.values.json）
- 容器：dingtalk-gateway（gateway.cli run --live-send --with-stream --live-read，
  host 网络，凭据只读挂载 /run/live-input/source-credentials.json）、
  dops-scheduler（Nacos 注册表 cron 驱动，17 条管线含 channel-daily-qudao 8:30）
- 仓库 e:/repos/digital-ops 分支 chore/p1-sync；全量 1408 例绿；CI=GitHub Actions
  三检查（python-lint/python-tests/frontend），main 保护=必须 PR
- 新报数机器人凭据：e:/repos/credentials/new-robot-credentials.json（gateway 用
  gateway-credentials.json，operator_id 借主应用）；7 群 ocid 见
  e:/repos/credentials/group-conversation-ids.json
- admin grant：bi_authz_grant 已有 王城 admin（014341566058939427），报数门禁
  跨区放行唯一例外

【第①步：投递恢复 + 实战验证】
【闸口 C1】运维在开发者后台给新应用（dingnlvj7z51xcoafbjl）开
  Card.Instance.Write（直达：https://open-dev.dingtalk.com/appscope/apply?content=dingnlvj7z51xcoafbjl#Card.Instance.Write）。
  开通后 outbox 中 pending 的 channel_daily 行会自动重试；也可
  python -m common.gateway.cli requeue --all --execute --confirm-local-test-write。
1. 验证投递：robot_outbox 行转 delivered；电商群收到三段式日报。
2. vanke 实战：万科群老师发「零售x 团购y」，核 fact_daily_report_offline
   两行（万科体验馆·零售/团购）+ 回执。
3. 杭州迁移：杭州群@机器人报数开通（region 配置已发布），发 §7.2 双轨公告；
   杭州是最后一个迁移群，切完旧机器人剔除。

【第②步：双跑收尾（checklist 第 2 步关闭）】
1. 现网杭州 20:00 催办未填名单 vs 云端 _unfilled（aliases 已含 陆芳琳→陆芳林；
   口径=通讯录全员）。
2. 旧渠道播报（渠道日报机器人）vs channel-daily ①段数字对照。
3. sync 时滞对策：机器人时刻表前必须有一轮 sync-dingtalk+extract-mart
   （调度加 17:25/19:25 两班或调整现有 cron），否则催办用陈数据——这是双跑
   实测结论，落进 scheduler 注册表。

【第③步：G4 及以后】
观察 2 个工作日 robot_outbox 全 delivered 无 failed →【闸口 G4】运维亲手注释
现网 Windows 5 个 cron（保留不删）→ 回退窗口 1 周 → G5 退役。

【已知待办（不阻塞主线，按优先级）】
- qudao/杭州/绍兴 月目标 → Nacos monthlyTargets（目标/达成率列与进度行激活）
- 电商 SKU 动销 sheet（业务侧按 spec §1.1 模板建）→ SKU 链路开发
- 宣蕾凤部门归属（报团购但不在体验中心子树，org.seed 待补）
- 电商责任人进 dim（org.seed 电商部门→qudao，18:30/20:00 提醒催办前置）
- celia 只读验收（B5，本地快照库执行）
- 榜单标题"9月21日 周二"新旧两链一致的显示习惯（非 bug，勿改）

【铁律（沿用，违反即返工）】
1. 凭据零入库零入仓；日志/plan 中 robotCode/AppSecret/密码即脱敏。
2. 同一时刻只跑一个 sync 进程；pkill 禁 -f 模式串（会自伤，先查 PID 再 kill）。
3. sync_runs 任何 failed/projection_pending 视为验收失败，查明重跑。
4. 改代码：本地改 → 全量测试绿（当前基线 1408）→ commit → PR → scp 两台。
5. 运维闸口（C1/G1-G5）必须等人工通过；生产群消息发送由 gateway 投递，
   agent 不用 webhook 直发（测试群演示除外）。
6. 严禁 git add -A；提交走 PR（main 受保护，三检查必须绿）。

【汇报】
续写 docs/superpowers/plans/2026-09-22-cloud-cutover-log.md：
投递恢复证据（outbox delivered 行）、vanke/杭州实战落库行、双跑对照表、
任何偏差登记。
````

---

## §B 一句话总结

**阶段4 G3 进行中**：容器链路全部常驻，绍兴已单轨，电商日报已接调度；当前唯一硬阻塞是新应用 `Card.Instance.Write` 权限（C1 闸口），开通后投递恢复 → vanke/杭州实战 → 双跑收尾 → G4 注释旧 cron。

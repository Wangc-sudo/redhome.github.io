# digital-ops 命令速查表

> 所有命令默认在仓库根目录 `E:\repos\digital-ops` 下执行。
> 凭据文件 `config.json` / `common/test_groups.json` 已 gitignore，不会被提交。

---

## 一、通用验证

```bash
# 运行全部单元测试
python -m unittest discover -s tests -t . -v

# Python 语法编译检查（common + 数字化）
python -m compileall common 数字化

# 查看当前 Git 变更
git status --short
```

---

## 二、测试群推送（TEST_MODE）

环境变量 `TEST_MODE=1` 会把所有机器人的目标群重定向到 `common/test_groups.json` 里的 `default` 群（功能验证群）。

```bash
# 渠道日报机器人（通用版）
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/main.py --date 2026-09-10

# 渠道日报机器人（真实表结构版 run_today）
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/run_today.py --date 2026-09-10

# 库存补货提醒
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/stock_alert.py --dry
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/stock_alert.py

# 采购入库提醒
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/purchase_alert.py --dry --hours 8
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/purchase_alert.py --hours 8

# 订单风险防控提醒
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/order_risk_alert.py --dry --today
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/order_risk_alert.py --today

# 热卖品监控
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/hot_items_monitor.py --dry
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/渠道日报机器人/hot_items_monitor.py

# 杭州日报机器人：手动跑 18:30 提醒到测试群
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/杭州日报机器人/hangzhou_reminder.py --remind

# 杭州日报机器人：手动跑 20:00 催办到测试群
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/杭州日报机器人/hangzhou_reminder.py --check

# 绍兴日报机器人：手动跑 18:30 提醒到测试群
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/shaoxing_daily_robot/shaoxing_reminder.py --remind

# 绍兴日报机器人：手动跑 20:00 催办到测试群
PYTHONIOENCODING=utf-8 TEST_MODE=1 python 数字化/钉钉/shaoxing_daily_robot/shaoxing_reminder.py --check
```

---

## 三、杭州日报机器人

目录：`数字化/钉钉/杭州日报机器人/`

```bash
# 查看当日填写状态
python hangzhou_reminder.py --status

# 手动跑 18:30 提醒（仅@未填人）
python hangzhou_reminder.py --remind

# 手动跑 20:00 催办（群消息 + DING 命令输出）
python hangzhou_reminder.py --check

# 查看组织档案
python org_sync.py --status

# 执行组织同步（通常由 cron 调用）
python org_sync.py --sync

# 数据体检
python check_data.py

# 重算合计行与达成率
python recalc_totals.py
```

---

## 四、绍兴日报机器人

目录：`数字化/钉钉/shaoxing_daily_robot/`

```bash
# 查看当日填写状态
python shaoxing_reminder.py --status

# 手动跑 18:30 提醒（仅@未填人）
python shaoxing_reminder.py --remind

# 手动跑 20:00 催办（群消息 + DING 命令输出）
python shaoxing_reminder.py --check

# 18:30/20:00 按当前小时自动分流（供 cron 用）
python shaoxing_reminder.py --once

# 查看组织档案
python org_sync.py --status

# 执行组织同步（通常由 cron 调用）
python org_sync.py --sync

# 数据体检
python check_data.py

# 重算合计行与达成率
python recalc_totals.py

# 榜单播报（生成 HTML + 发送群消息）
python leaderboard_report.py --send
```

---

## 五、渠道日报机器人

目录：`数字化/钉钉/渠道日报机器人/`

```bash
# 当前在用脚本：干跑看效果
python run_today.py --dry

# 当前在用脚本：指定日期推送
python run_today.py --date 2026-09-10

# 当前在用脚本：默认推送昨日
python run_today.py

# 通用版主入口（字段自动识别，备用方案）
python main.py --date 2026-09-10
python main.py --dry

# 库存补货提醒（复用缓存）
python stock_alert.py --dry
python stock_alert.py
python stock_alert.py --fresh  # 忽略缓存重新拉库存

# 采购入库提醒
python purchase_alert.py --dry --hours 8
python purchase_alert.py --hours 8

# 订单风险防控提醒
python order_risk_alert.py --dry --today
python order_risk_alert.py --today

# 热卖品监控
python hot_items_monitor.py --dry
python hot_items_monitor.py
```

---

## 六、表结构发现

目录：`数字化/钉钉/渠道日报机器人/`

```bash
# 列出 Base 下全部数据表
python discover_tables.py

# 列出指定表字段
python discover_tables.py -t 各渠道每日总销售 9

# 列出字段 + 看 5 条样例
python discover_tables.py -t 各渠道每日总销售 9 --sample 5
```

---

## 七、Git 本地流程

```bash
# 查看变更
 git status

# 查看 diff
 git diff

# 提交（按模块分批 add）
 git add <文件>
 git commit -m "type(scope): 简短描述"

# 不推送（定时任务只读本地 main，推送需单独确认）
# git push
```

---

## 八、常见排查

| 现象 | 命令/检查项 |
|------|------------|
| 群里没收到 | 先看脚本输出；Windows 控制台乱码时加 `PYTHONIOENCODING=utf-8` |
| 测试群 400 | 检查 `common/test_groups.json` 的 `robotCode` / `openConversationId` |
| 渠道机器人「未能识别销售额字段」 | 运行 `discover_tables.py` 确认字段名，或在 `config.json` 的 `fields.sales` 显式指定 |
| 单元测试发现 0 个 | 用 `python -m unittest discover -s tests -t . -v`，不要省略 `-t .` |

# 上云推进续接提示词（2026-09-22 断点快照）

> 用途：电脑重启/新会话后，把 §A 整段贴给执行 agent 即可无损续接。
> 主任务表：`docs/上云更新方案-2026-09-21.md` §3（唯一口径真源）。

---

## §0 断点状态（2026-09-22 晚）

**进度：方案 §3 走到第 7 步（T7 全链路冒烟）中途。**

| 步骤 | 状态 |
|---|---|
| 1 安全组 | ✅ 完成（三组专用 + RDS/DCS 放行 + 公网负向验证） |
| 2 网络/参数 | ✅ 完成（互 PING、Redis PING、RDS 三硬参数全过） |
| 3 RDS 建库 | ✅ 完成（四库 + yyhl-it 账号） |
| 4 live_safety 方案 B | ✅ 完成并提交（916ca14，1320 例绿） |
| 5 Nacos | ✅ dops-ctrl 上线（standalone+RDS+鉴权），种子已发布（12 pipelines + 15 dashboards） |
| 6 代码上机 | ✅ dops-app/ci 均有 /opt/dops/repo + venv + app.env |
| **7 冒烟** | 🔴 **卡在 sync-dingtalk：IPv6 出网不通导致源读取超时（根因已定位，修复命令已备好，未执行）** |
| 8 阶段4切换 | ⬜ 未开始（闸口 G1-G5，regions 真值未提供） |

**git 状态**：main 已 fast-forward 到 `9e6456c`（本地，**领先 origin/main 25 个提交**）；当前分支 `chore/p1-sync`（与 main 同点，领先 origin 14 个提交）；**两者都未 push**。工作区 6 个 tracked 脏条目（WDT 相关，非上云产出，归属待定）——`docker/integration/source-manifest.json`、`scripts/wdt_datasets.json` 为已修改；`docker/integration/backfill-watch.ps1`、`backfill-wdt-manifest.json`、`backfill-wdt-remaining.json`、`scripts/verbose_wdt_sync.py` 为**已删除态**。另有 4 个 untracked：本提示词、`docs/superpowers/specs/2026-09-22-live-sync-observability.md`、`docs/celia-prompt.md`、`frontend/bi-react/.vite/`（构建缓存）。`_tmp_probe_*.py` 等临时文件待清理。**切勿 `git add -A`**：会把 4 个删除与 `.vite/` 缓存一起固化，不可逆。

---

## §A 可复制执行提示词

> 复制以下整段给执行 agent。

````text
你是 cloud-ops-agent，续接 digital-ops 天翼云上云任务。断点：T7 冒烟的
sync-dingtalk 因 IPv6 出网问题卡住，修复方案已定，从「第一步」接着做。

【先读（5 分钟建立上下文）】
1. docs/上云推进续接提示词-2026-09-22.md（断点快照，本提示词出处）
2. docs/上云更新方案-2026-09-21.md（唯一口径真源，含 §2.6 已执行记录）
3. docs/云主机初始化与T2验证清单.md（P0-P3 已完成）
4. docs/阶段4切换派发提示词.md（第 8 步的契约，G1-G5 闸口）
5. docs/superpowers/specs/2026-09-22-live-sync-observability.md（T7 观测与验收设计）
6. docs/celia-prompt.md（只读查询 agent 提示词；其库名写死 mart_ops_test，为本地快照口径，云上为 mart_ops/raw_dingtalk/raw_wdt，切云前不要照搬）

【环境事实（已验证，直接用）】
- 天翼云华东1（南京），ctyun-cli 0.3.0 已装于 C:\Users\Y\.ctyun-cli\bin（密钥已配）
- 三台 ECS 免密 SSH（本机 ed25519 私钥直登 root）：
  dops-app=203.205.91.241（内网 .0.3）、dops-ctrl=203.205.93.194（.0.4）、dops-ci=203.205.93.195（.0.5）
- RDS：192.168.0.7:13049，root/yyhl-tset123，应用账号 yyhl-it/yyhl-it-01（四库已授权）
- Redis：192.168.0.6:6379，ACL 应用账号 yyhl-it/yyhl-it-01，管理账号 yyhl-tset/yyhl-tset123
- Nacos：192.168.0.4:8848（仅内网），控制台 nacos/12GvxoarqwsDlGQ0b1eA4A，
  凭据全量见 e:/repos/credentials/nacos-credentials.json
- dops-app：/opt/dops/repo（代码）、/opt/dops/venv（依赖已装）、/opt/dops/app.env（600）、
  /opt/dops/live/（source-credentials.json + source-manifest.json，600）
  ※ wdt 凭据段 9-22 上午曾为 26 字符占位符（sync-wdt 报 source_read_failed、16ms 即败），
  当日已用本地 e:/repos/credentials/source-credentials.trial.json 的真 wdt 段合并修复；
  dingtalk 段为另一来源真值，两段勿互相覆盖
- 仓库：e:/repos/digital-ops，分支 chore/p1-sync（=本地 main=9e6456c），全量测试 1320 例绿

【根因与修复（断点核心）】
sync 进程卡在 IPv6 TCP connect（2401:b180:2000:70::e:443，EINPROGRESS 永不完成）：
ECS 带 IPv6 地址、DNS 优先 AAAA，但 VPC 无公网 IPv6 出网，每个钉钉 API 请求
先等 IPv6 超时再回落 IPv4。修复 = gai.conf 优先 IPv4（可逆，不改代码）。

【第一步：修复并重跑 sync-dingtalk】
在 dops-app 执行：
  pkill -f "common.public_data.cli live-sync"   # 清掉卡死进程（若还在）
  grep -q "^precedence ::ffff:0:0/96  100" /etc/gai.conf || \
    echo "precedence ::ffff:0:0/96  100" >> /etc/gai.conf
并在 dops-ctrl、dops-ci 上同样补这行 gai.conf（gateway Stream、Nacos 客户端同坑）。
把卡死 run 查出来 UPDATE（注意主键列名是 sync_run_id，不是 run_id）：
  SELECT sync_run_id FROM mart_ops.sync_runs WHERE status='started';
  UPDATE mart_ops.sync_runs SET status='failed', failure_code='ipv6_egress_stall'
    WHERE status='started';
验证修复：/opt/dops/venv/bin/python -c "import time,httpx;t=time.time();
httpx.get('https://api.dingtalk.com/v1.0/oauth2/accessToken',timeout=15);
print(round(time.time()-t,2))" 应从 ~120s 降到 <1s。
然后重跑（后台+日志，单进程，绝不并发两个）：
  cd /opt/dops/repo && set -a && . /opt/dops/app.env && set +a
  export PUBLIC_DATA_CONFIG=/opt/dops/live/source-manifest.json
  nohup /opt/dops/venv/bin/python -m common.public_data.cli live-sync --live-read \
    --confirm-local-test-write --source dingtalk \
    --source-credentials /opt/dops/live/source-credentials.json \
    > /opt/dops/live/sync-dingtalk-3.log 2>&1 &
轮询：pgrep -f live-sync；mart_ops.sync_runs 状态机到 completed 为成功。
预期 10-30 分钟（28 个表分页读）。幂等，直接重跑无需清库。

【第二步：sync-wdt → extract-mart】
同一 env 下：
  live-sync --source wdt（145 个数据集，更久，同样后台+日志）
  extract-mart --confirm-local-test-write
验收（对齐阶段4手册第 2 步）：sync_runs 全 completed、无 failed/projection_pending；
连跑两轮 record_id_digest 与行数稳定；dim_robot_member 为全量通讯录口径，
行数应等于 org_directory 数据集的 records_read（现网 config.json 的
org.archives 43 人是旧机器人授权名单，属 bi_authz_grant 维度，不在此比对）。

【铁律（沿用，违反即返工）】
1. 凭据零入库零入仓；日志/plan 中出现 robotCode/AppSecret/密码即脱敏。
2. 同一时刻只跑一个 sync 进程（本轮事故就是双进程并发撞限流）。
3. sync_runs 里任何 failed/projection_pending 都视为验收失败，查明重跑，不带病前进。
4. 不改代码。若必须改：本地改 → 全量测试绿 → commit → scp 同步到 /opt/dops/repo。
5. 运维闸口 G1-G5（阶段4提示词）必须等人工通过，尤其 regions 真值与注释旧 cron。

【阻塞项与处理方案（2026-09-22 运维口径更新：B2 解除阻塞，B3 纳入 CI/CD）】
B1 regions 真值文件（运维提供，agent 不造不假）
   要什么：真实 robotCode/openConversationId/tableUrl，schema 同
   docker/integration/regions.seed.json；何时要：阶段4第 3 步真群对照前（T7 不需要）；
   给到后：走仓库外凭据渠道 → G2 闸口核对 → 真群对照。
B2 ECS 续费（已解除阻塞）：2026-09-22 运维定调——测试阶段到期不阻断任何工作，
   切换回退窗口前运维在控制台及时续费即可；agent 侧无动作，方案 §3 第 0 步
   硬约束同步放宽。
B3 git push（纳入 CI/CD，不再是人工时机决策）：
   push 即流水线入口——推 main 触发 .github/workflows/ci.yml（lint + 集成测试 +
   前端 typecheck/test/build），绿即代码可信；方案 §3 第 9 步 ECS-CI 顶替后
   推送即发布。当前 local main 领先 25、chore/p1-sync 领先 14，推前只需两件前置：
   ① B4 脏文件按下列方案收口；② 本地全量测试绿（当前基线 1320 例）。
B4 脏文件收口方案（B3 前置，一次性决策，禁止 git add -A）：
   4 个删除态（backfill-watch.ps1 / backfill-wdt-manifest.json /
   backfill-wdt-remaining.json / verbose_wdt_sync.py）：30 天回补的临时工装，
   建议确认删除，用 git add -u 精确固化；2 个修改（docker/integration/
   source-manifest.json、scripts/wdt_datasets.json）：回补实战沉淀的修正
   （含行键 purchase_no→order_no），diff 过目后提交；3 个新文档（本提示词 /
   observability spec / celia-prompt.md）：提交进仓（celia-prompt 无密钥，
   仅库名与 SQL 口径）；frontend/bi-react/.vite/ 构建缓存入 .gitignore；
   _tmp_probe_*.py 直接删除。
B5 celia 只读验收（排序已定）：task 1 排在 T7 冒烟之后、本地快照库
   （mart_ops_test）执行；T7 已通过，可随时派 celia。

【汇报】
续写 docs/superpowers/plans/2026-09-22-cloud-cutover-log.md（没有就新建）：
修复验证耗时对比、每轮 sync 的 sync_run_id/状态/行数对照、遇到的任何偏差。
````

---

## §B 一句话总结

**断点只有一件事**：dops-app（及另两台）加一行 `precedence ::ffff:0:0/96  100` 到 `/etc/gai.conf`，重跑 sync-dingtalk → sync-wdt → extract-mart，T7 就通了；其余全是已就绪状态。

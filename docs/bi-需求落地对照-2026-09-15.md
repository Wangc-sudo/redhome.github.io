# BI 需求落地对照（总经理 14 模块 × 11 页面）

日期：2026-09-15 · 输入：`e:/repos/BI看板颗粒度需求表.xlsx`（需求总表 / 看板页面结构 / 维度字典）
口径锚点：本表只做**数据现状盘点与落地分期**，卡片级设计回各页面 seed；派生口径一律后端算（见 `derived-metrics.md`）。

---

## 0. 顶层结构

业务分四大板块：**线下 / 电商 / 餐厅 / 万科&大莲花体验馆**（体验馆无定目标，财务单独出报表，仅月度）。
年度目标两套维度、各自成树不混加：

- 按板块：总 **76951 万** = 线下 21041 + 电商 54980 + 餐厅 930
- 按品牌：**习酒 61631 万**（跨板块口径），其中古韵＋大坛系列 **10000 万**（SKU 归属清单待品牌经理固化）

---

## 1. 模块 × 数据现状对照（实证于 mart_ops_test，2026-09-15）

| # | 模块 | 频率 | 数据来源（需求口径） | 现状 | 差距 / 阻塞 |
|---|------|------|----------------------|------|--------------|
| ① | 板块日销售额 | 日 T+1 | 各板块销售系统 | ✅ 电商 `fact_channel_daily_sales`、线下 `fact_daily_report_offline` 已在跑；L1 已有分板块 MTD 卡 | 餐厅日销无数据源（餐厅只有月度人工表）；波动阈值取行业默认：日环比销售额 ±20%、订单量 ±30%，大促期间切同比口径（BI 预警惯例：周期性业务同比优于环比），先入 `dim_threshold` 再共同确认 |
| ② | 销售结构下钻 | 日 T+1 | 各板块销售系统 | 🟡 电商可下钻到 SKU：`fact_order_line`（35441 行，含 shop/channel/brand/数量/实付→均价可算）× `dim_product`（3255 SKU，brand_name/series_name 层级天然存在） | **业务单元清单待各板块负责人提供**；线下目前只到部门/人，终端网点（5000+）无表 |
| ③ | 库存补货预警 | 日 | **WDT** `wms.StockSpec.search2` | 🟡 接口已探针验证（2026-09-15：仓 01 共 280 SKU，75 字段含可发库存/近 7~180 天动销/在途采购）；`stock_alert.py` 机器人生产在跑同源 | mart 无库存表：manifest 加数据集同步入 DB 即可；安全库存线取供应链通用公式默认：安全库存=近30天日均动销×15 天、补货点=日均动销×采购周期+安全库存（WDT 动销字段直接支持），采购确认两个参数后入 `dim_threshold` |
| ④ | 电商月度经营 | 月 | 运营月度报表 | 🟡 通道就绪：`manual_import` 模板 `ecommerce_monthly`（渠道×月：收入/毛利/费用，含合计对账） | **等运营给固定表头与每月出数日**，照模板填列即可零代码导入 |
| ⑤ | 餐厅月度经营 | 月 | 财务月度报表 | 🟡 通道就绪：模板 `restaurant_monthly`（门店×月） | 等财务给表头；门店清单不设枚举（业务自行增减） |
| ⑥ | 季度经营预实 | 季 | 财务季度预实分析表 | ❌ 无表无模板；`generic` 模板可承接 | 需财务给季度表表头；**dim_calendar 仅 2026-09，跨季聚合前必须先补日历**（roadmap P2 已立项） |
| ⑦ | 库存周转与库龄 | 月 | **WDT** 库存快照 + 入库单明细批次 | 🟡 批次字段已探针验证：`batch_no`/`expire_date`/`production_date`/`validity_days`；周转用快照 `num_month` 可算 | 与 ③ 同一次 WDT 接入解决；**白酒（酒精度≥10%vol）按 GB 7718 免标保质期，不设临期预警**，效期字段仅留批次追溯（串货/召回）；按帆软《全面库存管理方案》改做**库龄分级预警**：入库库龄分桶 0-90/91-180/181-365/>365 天，库龄>180 天金额占比>20% 亮黄、>365 天占比>10% 亮红并自动生成滞销清单（FineBI 账龄预警惯例；白酒行业库存周转 600-900 天为背景，确认时可上调） |
| ⑧ | 仓库运作 | 日 | **WDT** 出/入库单接口 | 🟡 已探针验证：`wms.stockout.Sales.queryWithDetail`（50 分钟窗 107 单，118 字段含拣货/打包/验货/分拣人、物流单号、重量）；采购/退货入库单接口已通 | manifest 加数据集即可采（窗口 ≤1h，复用现有 50min 切片机制）；超时定义取平台规则默认：付款→发货 >48h 为发货超时、发货→揽收 >24h 为揽收超时（天猫/京东/抖音/拼多多现货规则），WDT 人员字段可拆仓内分段 SLA；原因枚举给默认六类（缺货/爆单/揽收延迟/系统或面单异常/地址拦截/预售未到期），仓管增删确认 |
| ⑨ | 合同/方案/核销进度 | 周 | 合同台账/费用核销台账 | ❌ 无表 | 业务先回答：台账在哪、谁维护、核销截止日字段是否有；然后走 manual_import 或接系统 |
| ⑩ | 资金安全预警 | 月 | 财务资金报表 | ✅ **五张表已在 mart**：应收账龄 81 行、店铺资金 300、预付发票 52、线下保证金 55、平台保证金 60 | 只缺页面对账口径：各类账期/超期标准需财务定义后写入卡 |
| ⑪ | 体验馆月度经营 | 月 | 体验馆经营台账 | 🟡 克隆 `restaurant_monthly` 成 `showroom_monthly` 即可（零代码） | 与体验馆负责人确认出数人与口径；首年无同比基数 |
| ⑫ | 年度目标达成 | 日 T+1 | 各板块汇总 | 🟡 L1 已有年度进度卡（按电商年目标 76021 万） | **目标口径要改**：板块树 76951 万分板块 + 品牌树习酒 61631/古韵大坛 10000；`dim_target` 目前只到人员月粒度，需补板块年粒度与品牌年粒度（roadmap P2 已记渠道 grain 缺口） |
| ⑬ | 月度销售同比 | 月 | 2025 年月度销售报表 | ❌ 无 2025 基数；日历只到 2026-09 | 两件事：2025 报表走 `generic` 模板首次导入（需对齐含税/退货口径）；日历扩到 2025 全年。2026 春节 2/17 vs 2025 春节 1/29 错位，页面须附 1-2 月合并口径提示 |
| ⑭ | 日环比 | 日 T+1 | 同 ① | ✅ L1 已有线下/电商日环比卡（trend7） | 基期=前一自然日已定；餐厅缺日数据源同 ① |

> ⑩⑪ 在口述十二问之外，以需求总表 14 编号为准。

---

## 2. 页面结构（11 页，L1 粗 / L2 深）

| 页面 | 层级 | 模块 | 数据就绪度 |
|------|------|------|-----------|
| 首页·经营驾驶舱 | L1 | ①③⑩⑫⑭ | 🟡 销售/目标已有；③缺库存源；⑩表已就绪 |
| 销售分析页 | L2 | ①② | 🟡 电商侧可立即做（订单行×商品维）；线下等业务单元清单 |
| 电商经营页 | L2 | ④ | 🟡 等运营月报首填 |
| 餐厅经营页 | L2 | ⑤ | 🟡 等财务月报首填 |
| 季度预实分析页 | L2 | ⑥ | ❌ 日历 + 财务季度表双阻塞 |
| 库存与库龄页（原"效期"按白酒改库龄） | L2 | ③⑦ | 🟡 WDT 探针通过（2026-09-15），排期同步入 DB 即可做 |
| 仓储运作页 | L2 | ⑧ | 🟡 WDT 探针通过（2026-09-15），排期同步入 DB 即可做 |
| 合同与核销页 | L2 | ⑨ | ❌ 等业务盘点台账 |
| 资金安全页 | L2 | ⑩ | ✅ **数据已在库，可直接排期** |
| 体验馆月度经营页 | L2 | ⑪ | 🟡 克隆模板即可 |
| 月度同比分析页 | L2 | ⑬ | ❌ 2025 基数 + 日历双阻塞 |

---

## 3. 维度字典落库建议（7 维，切法不再加细）

| 维度 | 落点 | 现状 |
|------|------|------|
| 业务板块 | 卡片/页面级固定过滤，不进 dim | 枚举固定四类 |
| 业务单元 | 新建 `dim_business_unit`（板块→单元） | **等清单**，IT 反向盘点后固化 |
| 品牌→系列→SKU | 复用 `dim_product.brand_name/series_name/spec_no` | ✅ 已有；习酒古韵/大坛归属清单品牌经理确认后即可做系列目标 |
| 渠道 | 线下渠道枚举（流通/宴席/商超/团购/体验馆）进 seed 选项源 | 电商侧已有；线下侧待盘点现有报表 |
| 客户/终端网点 | `dim_terminal`（5000+） | 无表，随线下明细接入 |
| 仓库/批次 | 随 WDT 接入建 `fact_inventory_snapshot`；仓库档案 `setting.Warehouse.queryWarehouse` 已通（50 仓） | 无表，接口已验证 |
| 时间 | `dim_calendar` | **仅 2026-09，扩日历是季度/同比的前置立项** |

---

## 4. 落地节奏

需求表原文建议：先只上首页驾驶舱（①③⑫）跑 2 周，数据对准后再铺 L2。结合数据就绪度修正为：

- **P0（数据已齐，直接排期）**
  1. 资金安全页（⑩）—— 五表已在 mart，只差财务确认超期标准
  2. 首页驾驶舱改造 —— 三板块日销/日环比（⑭）+ 年度目标双树（⑫，需 `dim_target` 补板块/品牌年粒度）
  3. 销售分析页电商侧（②）—— 订单行 × 商品维，品牌→系列→SKU + 均价
- **P1（通道就绪，等业务首填）**：电商经营页④、餐厅经营页⑤、体验馆页⑪（克隆模板）、2025 基数导入（⑬ 前半）
- **P2（需立项接系统）**：库存/库龄③⑦ + 仓储⑧（**WDT，2026-09-15 探针全通**：工作量 = manifest 加数据集 + allowlist 扩充 + mart 投影，不再需要 ERP/WMS 立项）、合同核销⑨（先业务盘点）、日历跨年（⑥⑬ 公共前置）
- **横向底座**：标准图形库（柱/条/线/环四件套 + 主题 token），杜绝卡片各自写裸 ECharts

## 5. 等业务/跨部门输入清单（阻塞项汇总）

阈值口径类均有行业标准默认值，按下表先行落地为 `dim_threshold` seed，业务确认或改数即可；清单/报表类无行业替代，仍需业务提供。

### 5.1 阈值口径类：行业默认值先行（IT 落地，业务确认）

| 事项 | 模块 | 默认值（出处） | 确认人 |
|------|------|----------------|--------|
| 波动预警阈值 | ① | 日环比销售额 ±20%、订单量 ±30%；大促切同比（BI 预警惯例：周期性业务同比优于环比） | 总经理+板块 |
| 安全库存/补货点 | ③ | 安全库存=近30天日均动销×15 天；补货点=日均动销×采购周期+安全库存（供应链通用公式） | 采购 |
| 库龄分桶与滞销线 | ⑦ | 白酒免标保质期、**不设临期**；入库库龄分桶 0-90/91-180/181-365/>365 天；>180 天金额占比>20% 黄、>365 天占比>10% 红（帆软《全面库存管理方案》+ FineBI 账龄预警惯例） | 采购 |
| 发货/揽收超时 | ⑧ | 付款→发货 >48h、发货→揽收 >24h（天猫/京东/抖音/拼多多现货规则）；仓内分段 SLA 默认：审单 4h/拣货 4h/打包 2h/发运 12h | 仓管 |
| 超时原因枚举 | ⑧ | 默认六类：缺货/爆单产能不足/揽收延迟/系统或面单异常/地址修改拦截/预售未到期 | 仓管 |
| 账期/超期标准 | ⑩ | 应收账龄表已在 mart，超期线（如 >60 天）待财务定 | 财务 |

### 5.2 清单/报表类：必须业务输入

| 事项 | 找谁 | 阻塞模块 |
|------|------|---------|
| 业务单元清单 | 各板块负责人 | ② |
| 运营月报固定表头 | 电商运营 | ④ |
| 财务月报/季度预实表头 | 财务 | ⑤⑥ |
| 合同核销台账位置与维护人 | 线下/电商运营 | ⑨ |
| 古韵/大坛 SKU 归属清单 | 品牌经理 | ⑫ |
| 2025 年月度销售报表 | 财务/各板块 | ⑬ |

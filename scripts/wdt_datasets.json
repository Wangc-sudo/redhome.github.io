{
  "lookback_days": 1,
  "window_minutes": 50,
  "datasets": [
    {
      "dataset": "wdt_trade_detail",
      "method": "sales.TradeQuery.queryWithDetail",
      "record_id_path": "trade_no",
      "page_size": 100,
      "max_pages": 1000,
      "window": "split",
      "params": {"time_type": "2"}
    },
    {
      "dataset": "wdt_purchase_orders",
      "method": "wms.stockin.Purchase.queryWithDetail",
      "record_id_path": "purchase_no",
      "page_size": 100,
      "max_pages": 1000,
      "window": "split",
      "params": {}
    },
    {
      "dataset": "wdt_stockout_sales",
      "method": "wms.stockout.Sales.queryWithDetail",
      "record_id_path": "order_no",
      "page_size": 100,
      "max_pages": 1000,
      "window": "split",
      "params": {"status_type": 0}
    },
    {
      "dataset": "wdt_stockin_refund",
      "method": "wms.stockin.Refund.queryWithDetail",
      "record_id_path": "order_no",
      "page_size": 100,
      "max_pages": 1000,
      "window": "split",
      "params": {}
    },
    {
      "dataset": "wdt_stock_specs",
      "method": "wms.StockSpec.search2",
      "record_id_path": "rec_id",
      "page_size": 100,
      "max_pages": 200,
      "window": "split",
      "params": {}
    }
  ],
  "disabled": [
    {
      "dataset": "wdt_goods_catalog",
      "method": "goods.Goods.queryWithSpec",
      "record_id_path": "goods_id",
      "page_size": 100,
      "max_pages": 200,
      "window": "split",
      "params": {"hide_deleted": 1},
      "_reason": "Root cause of the old status=99 failures found 2026-09-12: 'goods_query' is the qyb (enterprise) edition method name; our WdtClient hits the qjb (flagship) gateway. Correct flagship method is goods.Goods.queryWithSpec. Live probe also showed the method REQUIRES start_time/end_time params with windows of at most 30 days (empty params -> status 100), so time_boxed must stay true. Kept disabled: dim_product is sourced directly by common/public_data/product_catalog.py, and this generic pipeline splits windows by the global 50-minute window_minutes, which is a poor fit for a rarely-changing catalog; re-enable only if the goods catalog also needs to land in wdt_records."
    }
  ]
}

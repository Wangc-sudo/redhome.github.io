PyMySQL==1.1.1
cryptography==50.0.1
dingtalk-stream==0.24.3
nacos-sdk-python==0.1.12
PyYAML==6.0.2
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1
# bi-web 阶段 2 卡片缓存的 Redis 后端（可选增强；不配置
# PUBLIC_DATA_REDIS_URL 时惰性到永不导入）。
redis==5.2.1
# 人工报表导入通道读 XLSX（CSV 不需要；仍为惰性导入，
# 见 docs/manual-import-channel.md）。
openpyxl==3.1.5

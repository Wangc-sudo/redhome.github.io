"""人工报表导入通道（C 类数据源）。

需求 ④⑤⑥⑪⑬（电商/餐厅月度经营、季度预实、体验馆月度经营、月度同比）
的共同瓶颈是：毛利、费用、预算、2025 历史基数都是**人工报表**，没有进库
通道。本包提供一条与 source-agnostic 的通用导入底座：

    模板（YAML，版本受控） → loader（CSV/XLSX） → validate（校验报告）
        → repository（raw_manual 幂等落库） → projector（mart_ops 窄表）

设计取舍（与既有管线保持一致）：

* **模板即契约**：源列表头千变万化，标准字段稳定，映射写在版本受控的
  YAML 里，业务改表头只需改模板，不改代码；
* **raw 存通用窄行**：不按模板动态建表——动态 DDL 会让 live_migrations
  的校验和随模板漂移。原始行以 JSON 原样落 ``manual_import_row``，
  模板演进不牵动库结构；
* **默认 dry-run**：落库必须显式 ``--apply``，先给人看校验报告；
* **幂等**：同一文件重复导入无副作用；同一 dataset+period 换文件重导，
  覆盖而非追加。

本包不读凭据、不发网络请求，只依赖本地文件与两个数据库连接。
"""

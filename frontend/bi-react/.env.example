# bi-react 环境变量样例：复制为 .env.local 后按需填写（.env.local 不入库）
#
# 后端 Bearer token，与后端 BI_WEB_TOKEN 保持一致。
# 未设置 / 为空 = 后端全开放（后端 app.py: unset or empty means fully open）。
# dev 下由 vite.config.ts 的 proxy 读取并注入 Authorization 头，浏览器侧无需处理。
BI_WEB_TOKEN=

# VITE_MOCK=1 → 不请求后端，直接用 src/data/mock/fixtures.ts 的 CubeSchema fixture
# （后端未启动也能开发/演示）。留空或 0 = 走真实 /api/v1。
VITE_MOCK=0
